# mono-agent

Агент, який відповідає на питання про власні гроші й про правила банку, спираючись
на живий API monobank і на 18 публічних документів банку — і не вигадує відповідь,
коли даних немає.

> «Скільки я витратив учора?» · «Що з моєю банкою на відпустку?» ·
> «Я загубив картку, що робити?» · «Яка комісія за переказ в інший банк?»

Два джерела правди, і жодного слова від себе: цифри беруться з виписки, правила —
з чинних Умов і Тарифів із посиланням на пункт. Питання, відповіді на яке немає в
жодному з двох джерел, закінчується словами «цього немає», а не правдоподібним
переказом.

Задача навмисно така, яку **не закрити одним викликом API**: у monobank виписка
запитується не за назвою картки, а за `id` рахунку, і цей `id` віддає лише
окремий метод. Тому кожна відповідь про витрати — це ланцюжок мінімум із двох
кроків, де результат першого є аргументом другого.

```
list_accounts()  →  id рахунку  →  get_statement(account, from_ts, to_ts)  →  відповідь
```

## Як запустити

```bash
python3.11 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
```

У `.env` вписати два значення:

- `ANTHROPIC_API_KEY` — з console.anthropic.com, `ANTHROPIC_BASE_URL` лишити порожнім
- `MONO_TOKEN` — на https://api.monobank.ua (вхід за QR через застосунок monobank,
  там же кнопка отримання персонального токена)

Ключі читаються **тільки з `.env`**, у коді їх немає, `.env` у `.gitignore`.

Далі — база документів. Корпус уже лежить у `knowledge/`, але векторний індекс
треба побудувати локально (перший запуск тягне ваги моделі ембедингів — 2,1 ГБ
у кеші, ключа для неї не потрібно):

```bash
python search.py --build      # ~84 с на MacBook, одноразово
```

Щоб перезібрати сам корпус із сайту банку — `python fetch_knowledge.py`.

### Інший постачальник моделі

Агент говорить протоколом Anthropic Messages, тому працює з будь-яким сервісом,
який цей протокол реалізує — код при цьому не змінюється. Наприклад QwenCloud:

```
ANTHROPIC_API_KEY=<ключ QwenCloud>
ANTHROPIC_BASE_URL=https://token-plan.ap-southeast-1.maas.aliyuncs.com/apps/anthropic
ANTHROPIC_MODEL=qwen3.8-max
```

Змінна називається `ANTHROPIC_API_KEY`, бо означає «ключ до Anthropic-сумісного
API», а не «ключ, виданий Anthropic». Порожній `ANTHROPIC_BASE_URL` означає
стандартний `api.anthropic.com`.

`.env` читається з `override=True`: якщо в оточенні вже є `ANTHROPIC_BASE_URL`
(його виставляють деякі інструменти), налаштування з файлу мусить перемагати,
інакше запити тихо підуть не туди.

### Команди

```bash
python run.py "скільки я витратив учора"
python run.py "що з моєю банкою" --verbose      # показати сирі відповіді інструментів
python run.py "скільки я витратив за літо" --max-turns 3
python run.py "порахуй витрати на каву" --tools v1
python run.py "скільки я витратив учора" --mask  # приховати суми та id
python run.py "я загубив картку, що робити"      # питання до бази документів
python run.py "як оформити пенсію" --rag naive   # база знань без запобіжників
```

| Прапорець | Навіщо |
|---|---|
| `--tools v1\|v2` | варіант описів інструментів (див. челендж A) |
| `--rag naive\|guarded` | поведінка пошуку по документах (див. челендж Б) |
| `--max-turns N` | ліміт кроків циклу |
| `--model NAME` | інша модель |
| `--verbose` | повні відповіді інструментів, не тільки підсумок |
| `--mask` | замінити цифри в сумах і скоротити `id` — для публікації прогонів |

`--mask` вимкнено за замовчуванням: щодня потрібні справжні числа. Маска чіпає
лише друк — модель у будь-якому разі отримує реальні дані, тому замаскований
прогін лишається справжнім прогоном, а не переказом.

Усі сцени разом відтворює `demo.py`:

```bash
python demo.py          # усі сцени
python demo.py 3 6      # тільки вибрані
python demo.py --mask   # з прихованими сумами
```

| Сцена | Що показує |
|---|---|
| 1 | щасливий ланцюжок `list_accounts` → `get_statement` |
| 2 | `tool_error`: недійсний токен monobank |
| 3 | `turns_exhausted`: питання про літо при ліміті 3 кроки |
| 4 | `api_error`: навмисно зіпсований ключ |
| 5 | `no_tool_used`: питання про самого агента |
| 6 | челендж A: той самий запит на описах `v1` і `v2` |
| 7 | порожній період: `count: 0` замість вигаданих операцій |
| 8 | питання до бази документів, поставлене іншими словами |
| 9 | ембединги проти BM25 на однакових даних (без моделі) |
| 10 | челендж Б: питання не з бази на `naive` і `guarded` |

## Який API використано

[monobank personal API](https://api.monobank.ua/docs/), автентифікація заголовком
`X-Token`. Факти нижче звірені зі вбудованою OpenAPI-специфікацією (версія `v250818`),
а не взяті з памʼяті.

| Ендпоінт | Що дає | Обмеження |
|---|---|---|
| `GET /personal/client-info` | рахунки (`id`, `type`, `currencyCode`, `balance`) і банки (`jars`) | 1 запит / 60 с |
| `GET /personal/statement/{account}/{from}/{to}` | виписка за період | 1 запит / 60 с; вікно ≤ 2 682 000 с (31 доба + 1 год) |

Дві деталі API визначили дизайн агента:

- **суми приходять у мінімальних одиницях** (копійках), відʼємні — витрати;
  ділення на 100 лишається за моделлю, тому в кожній відповіді інструмента
  є явна примітка про формат;
- **помилки треба читати за HTTP-кодом**, тіло містить `errorDescription`.
  Ліміт 1 запит/60 с означає, що `429` — не екзотика, а звичайний робочий стан,
  який агент мусить пояснити людині.

# Модуль 1 · Основа: ланцюжок інструментів

## Ланцюжок

| Інструмент | Аргументи | Повертає |
|---|---|---|
| `list_accounts` | — | рахунки і банки з `id`, валютою, балансом |
| `get_statement` | `account`, `from_ts`, `to_ts` | операції за період |

`get_statement` не можна викликати першим, і це забезпечено **кодом**, а не
проханням в описі: інструмент приймає лише той `id`, який справді прийшов від
`list_accounts` у цьому ж прогоні. Будь-що інше — вигаданий `id`, назва картки,
`0` з документації monobank — повертає `unknown_account`. Реєстр відомих `id`
очищається на початку кожного прогону, тож ланцюжок доводиться проходити щоразу.

## Агент не вигадує даних

Порожній період — це `count: 0` з приміткою, а не помилка: розрізняти
«операцій не було» і «дані не вдалося отримати» агент зобовʼязаний уголос.
Виписка довша за 150 операцій позначається `truncated` з попередженням, щоб
неповна сума не видавалась за повну. У системному промпті числа й назви
дозволено брати виключно з відповідей інструментів.

## Стани агента

Цикл завжди завершується одним із пʼяти станів, і це **повернене значення**,
а не побічний ефект — викликач розгалужується на ньому, а не вгадує з тексту.

| Стан | Коли |
|---|---|
| `ok` | ланцюжок пройдено, відповідь спирається на дані |
| `tool_error` | жоден виклик інструмента не вдався |
| `turns_exhausted` | вичерпано ліміт кроків |
| `api_error` | збій моделі: ключ, мережа, ліміт |
| `no_tool_used` | модель відповіла, жодного разу не звернувшись до банку |

Прогін, який оговтався після невдалого виклику, завершується як `ok`, але зі
списком `failures`: часткова халепа лишається видимою, не стаючи фатальною.

## Ліміт кроків

`MAX_TURNS` (за замовчуванням 6) обмежує кількість обертів циклу. Вичерпання —
не тихий обрив: цикл робить фінальний виклик **без інструментів** і просить
модель підсумувати зібране та назвати, що лишилось недоступним. Якщо падає і
цей виклик — повертається фіксована фраза. У будь-якому разі користувач
отримує відповідь і стан `turns_exhausted`, а не мовчання.

## Прогони

Усі прогони зняті на QwenCloud (`qwen3.8-max`) через Anthropic-сумісний
ендпоінт — код той самий, що піде з ключем Anthropic. Суми і `id` приховані
прапорцем `--mask`; модель у прогоні отримувала справжні дані, маска чіпає
лише друк.

### 1. ok — щасливий ланцюжок

```
Запит: «Скільки я витратив за минулий тиждень?»
Модель: qwen3.8-max · інструменти: v2 · ліміт кроків: 6

  [1] list_accounts {}
      → 5 рахунків, 0 банок
  [2] get_statement {"account": "suG_…", "from_ts": 1786309228, "to_ts": 1786914027}
      → 0 операцій
  [3] get_statement {"account": "P9a3…", "from_ts": 1786309228, "to_ts": 1786914027}
      → 0 операцій
  [4] get_statement {"account": "Vo2p…", "from_ts": 1786309228, "to_ts": 1786914027}
      → 17 операцій
  [5] get_statement {"account": "qeN_…", "from_ts": 1786309228, "to_ts": 1786914027}
      → 0 операцій
  [6] get_statement {"account": "BgYH…", "from_ts": 1786309228, "to_ts": 1786914027}
      → 0 операцій

Стан: ok · кроків: 6 · 56.17 с · токени 4923→3069

За минулий тиждень (10–16 серпня) ваші витрати становили ####,## грн — усі
з гривневої картки *####. На USD, EUR та інших рахунках операцій не було.

Найбільші витрати: Каті — #### грн (двома переказами), Claude — ####,## грн,
BradoBrey — ### грн, Rozetka і RozetkaPay — разом ####,## грн, AliExpress —
###,## грн, Ant*Alibaba Cloud — ###,## грн, Київстар — ### грн, комунальні
платежі — близько ### грн.
```

Ланцюжок пройдено: `id` рахунків узято з першого виклику й підставлено в
наступні. Порожні рахунки названо порожніми.

### 2. tool_error — токен monobank недійсний

```
Запит: «Скільки я витратив за минулий тиждень?»
Модель: qwen3.8-max · інструменти: v2 · ліміт кроків: 6

  [1] list_accounts {}
      ✗ forbidden: Unknown 'X-Token'

Стан: tool_error · кроків: 1 · 18.24 с · токени 1774→810
Збої інструментів: ['forbidden']

Не вдалося отримати дані: банк відхилив запит із помилкою доступу
(«Unknown X-Token») — схоже, токен авторизації недійсний або не дає доступу
до цих даних. Оновіть, будь ласка, токен доступу, і я порахую ваші витрати
за минулий тиждень.
```

Жодної вигаданої суми: агент назвав причину і сказав, що робити.

### 3. turns_exhausted — вичерпано ліміт кроків

```
Запит: «Скільки я витратив за все літо?»
Модель: qwen3.8-max · інструменти: v2 · ліміт кроків: 3

  [1] list_accounts {}
      → 5 рахунків, 0 банок
  [2] get_statement {"account": "Vo2p…", "from_ts": 1780261228, "to_ts": 1782853228}
      → 59 операцій
  [3] get_statement {"account": "Vo2p…", "from_ts": 1782853228, "to_ts": 1785531628}
      → 57 операцій
  [4] get_statement {"account": "Vo2p…", "from_ts": 1785531628, "to_ts": 1787247028}
      ✗ rate_limited: Too many requests
  [5] get_statement {"account": "suG_…", "from_ts": 1780261228, "to_ts": 1782853228}
      → 0 операцій
  [6] get_statement {"account": "suG_…", "from_ts": 1782853228, "to_ts": 1785531628}
      → 0 операцій
  [7] get_statement {"raw_arguments": "{\"account\": \"suG_a26kHNu4uwcO72KB-Q\", \"from_ts\": 1785531628, \"to_ts\": 17872"}
      ✗ bad_args: get_statement() got an unexpected keyword argument 'raw_arguments'
  [8] get_statement {"account": "Vo2p…", "from_ts": 1785531628, "to_ts": 1787247028}
      → 30 операцій
  [9] get_statement {"account": "suG_…", "from_ts": 1785531628, "to_ts": 1787247028}
      → 0 операцій
  [10] get_statement {"account": "P9a3…", "from_ts": 1780261228, "to_ts": 1782853228}
      → 0 операцій
  [11] get_statement {"account": "P9a3…", "from_ts": 1782853228, "to_ts": 1785531628}
      → 0 операцій
  [12] get_statement {"account": "P9a3…", "from_ts": 1785531628, "to_ts": 1787247028}
      ✗ rate_limited: Too many requests

Стан: turns_exhausted · кроків: 12 · 112.54 с · токени 24462→5307
Збої інструментів: ['rate_limited', 'bad_args', 'rate_limited']

Не вклався у відведену кількість кроків і не встиг зібрати дані.
Звузьте період або спитайте про один рахунок.
```

Три оберти циклу вмістили 12 викликів інструментів — модель просить кілька
одразу. Ліміт спрацював: замість мовчання користувач отримав фразу про те,
що саме не вдалося, і що робити далі.

У цьому ж прогоні видно два реальні збої, яких не було в плані: **справжній
`rate_limited` від monobank** (ліміт 1 запит/60 с таки діє при інтенсивному
навантаженні) і **обрізаний JSON аргументів** від моделі — `dispatch` упіймав
його як `bad_args` і цикл поїхав далі замість падіння.

### 4. api_error — постачальник моделі відмовив

```
Запит: «Скільки я витратив учора?»
Модель: qwen3.8-max · інструменти: v2 · ліміт кроків: 6

  інструменти не викликались

Стан: api_error · кроків: 0 · 1.04 с · токени 0→0
Помилка: AuthenticationError: Error code: 401 - {'request_id':
'539f7f6e-fadc-4068-af71-55b987c77933', 'code': 'InvalidApiKey', 'message':
'Invalid API-key provided. For details, see:
https://www.alibabacloud.com/help/en/model-studio/error-code#apikey-error'}

Сервіс моделі недоступний, відповісти зараз не можу.
```

Раніше цей самий стан спостерігався й без інсценування — коли вичерпалась
тижнева квота плану, API повернув `429 Throttling.AllocationQuota`.

### 5. no_tool_used — питання не про дані

```
Запит: «Привіт! Розкажи, що ти вмієш і як тобою користуватись.»
Модель: qwen3.8-max · інструменти: v2 · ліміт кроків: 6

  інструменти не викликались

Стан: no_tool_used · кроків: 0 · 9.78 с · токени 1037→365

Привіт! Я асистент по особистих фінансах monobank. Ось що я вмію:

1. Показати ваші рахунки та картки разом із валютою і поточним балансом.
2. Показати виписку операцій по конкретному рахунку за період до 31 дня.
3. Порахувати підсумки за випискою: витрати, надходження, баланс за період.

Важливо: точний id рахунку я беру зі списку ваших рахунків, тож для виписки
по конкретній картці я спочатку перегляну список рахунків, а потім витягну
виписку. Якщо період більший за 31 день, я розіб'ю його на кілька виписок.
```

Стан окремий саме тому, що така відповідь **не спирається на дані банку** —
і це має бути видно викликачу, а не ховатись за `ok`.

### Порожній період — доказ, що агент не вигадує

```
Запит: «Що я купував сьогодні між 3 і 4 ранку?»
Модель: qwen3.8-max · інструменти: v2 · ліміт кроків: 6

  [1] list_accounts {}
      → 5 рахунків, 0 банок
  [2] get_statement {"account": "suG_…", "from_ts": 1787184021, "to_ts": 1787187621}
      → 0 операцій
  [3] get_statement {"account": "P9a3…", "from_ts": 1787184021, "to_ts": 1787187621}
      → 0 операцій
  [4] get_statement {"account": "Vo2p…", "from_ts": 1787184021, "to_ts": 1787187621}
      → 0 операцій
  [5] get_statement {"account": "qeN_…", "from_ts": 1787184021, "to_ts": 1787187621}
      → 0 операцій
  [6] get_statement {"account": "BgYH…", "from_ts": 1787184021, "to_ts": 1787187621}
      → 0 операцій

Стан: ok · кроків: 6 · 22.16 с · токени 3320→992

Сьогодні між 3:00 і 4:00 ранку жодних покупок не було — за цей період на всіх
ваших рахунках операцій немає.
```

Порожня виписка — не помилка і не привід домальовувати найближчі покупки.

# Модуль 1 · Челендж A: опис інструмента = поведінка агента

Знайти запит, на якому агент плутає аргументи, полагодити це **описом
інструмента**, і показати прогони до і після.

**Запит:** «Скільки я витратив за останні три місяці?»

**Аргумент, який плутається:** `to_ts` у `get_statement`. monobank віддає
виписку максимум за 31 добу + 1 годину (2 682 000 с). Опис `v1` про це мовчить —
там сказано лише «Останній час виписки», — тож агент замовляє одразу весь
тримісячний період.

**Що змінюється між варіантами.** Виконуваний код інструментів **однаковий**:
та сама функція, та сама валідація, той самий HTTP-запит. Відрізняється рівно
текст опису — тому різниця в поведінці пояснюється описом, а не прихованою
правкою логіки. Перемикається прапорцем `--tools v1|v2`.

### Перша гіпотеза не підтвердилась

Спершу я очікував іншої помилки: специфікація monobank дозволяє передати
`account="0"` («дефолтний рахунок»), і `v1` цю можливість згадує. Здавалося,
агент спокуситься й пропустить `list_accounts`. Прогін це спростував — на
запит «Скільки я витратив у доларах за минулий тиждень?» агент і на `v1`
чесно взяв `id` доларової картки зі списку рахунків.

Тому челендж побудований на помилці, яка справді сталася, а не на очікуваній.

## Опис до (`v1`) — формулювання з офіційної доки

```
get_statement:
  Отримання виписки за період.
    account — Ідентифікатор рахунку або банки з переліку, або 0 — дефолтний рахунок.
    from_ts — Початок часу виписки.
    to_ts   — Останній час виписки.
```

## Прогін до

```
Запит: «Скільки я витратив за останні три місяці?»
Модель: qwen3.8-max · інструменти: v1 · ліміт кроків: 6

  [1] list_accounts {}
      → 5 рахунків, 0 банок
  [2] get_statement {"account": "Vo2p…", "from_ts": 1779470650, "to_ts": 1787246650}
      ✗ window_too_large: Період 7776000 с перевищує ліміт monobank 2682000 с (31 доба + 1 год)
  [3] get_statement {"account": "suG_…", "from_ts": 1779470650, "to_ts": 1787246650}
      ✗ window_too_large: Період 7776000 с перевищує ліміт monobank 2682000 с (31 доба + 1 год)
  [4] get_statement {"account": "P9a3…", "from_ts": 1779470650, "to_ts": 1787246650}
      ✗ window_too_large: Період 7776000 с перевищує ліміт monobank 2682000 с (31 доба + 1 год)
  [5] get_statement {"account": "qeN_…", "from_ts": 1779470650, "to_ts": 1787246650}
      ✗ window_too_large: Період 7776000 с перевищує ліміт monobank 2682000 с (31 доба + 1 год)
  [6] get_statement {"account": "BgYH…", "from_ts": 1779470650, "to_ts": 1787246650}
      ✗ window_too_large: Період 7776000 с перевищує ліміт monobank 2682000 с (31 доба + 1 год)
  [7] get_statement {"account": "Vo2p…", "from_ts": 1784654650, "to_ts": 1787246650}
      → 45 операцій
  [8] get_statement {"account": "Vo2p…", "from_ts": 1782062650, "to_ts": 1784654650}
      → 57 операцій
  [9] get_statement {"account": "Vo2p…", "from_ts": 1779470650, "to_ts": 1782062650}
      → 66 операцій

Стан: ok · кроків: 9 · 61.89 с · токени 14677→3387
Збої інструментів: ['window_too_large', 'window_too_large', 'window_too_large',
                    'window_too_large', 'window_too_large']
```

Агент не зрозумів обмеження з першого разу: він повторив ту саму помилку
**пʼять разів поспіль**, по одному на кожен рахунок, і лише потім розбив
період на частини.

## Опис після (`v2`)

```
get_statement:
  Крок 2 з 2. Виписка по ОДНОМУ рахунку за період.
  Викликати лише після list_accounts, взявши id звідти.
    account — id рахунку або банки, скопійований з відповіді list_accounts.
              Не назва картки, не валюта, не номер картки.
    from_ts — Початок періоду. Unix-час у СЕКУНДАХ (10 цифр), не мілісекунди
              і не рядок з датою.
    to_ts   — Кінець періоду, Unix-час у секундах. Максимальне вікно —
              2682000 с (31 доба). Довший період розбивати на кілька викликів.
```

## Прогін після

```
Запит: «Скільки я витратив за останні три місяці?»
Модель: qwen3.8-max · інструменти: v2 · ліміт кроків: 6

  [1] list_accounts {}
      → 5 рахунків, 0 банок
  [2] get_statement {"account": "Vo2p…", "from_ts": 1779200771, "to_ts": 1781882771}
      → 70 операцій
  [3] get_statement {"account": "Vo2p…", "from_ts": 1781882771, "to_ts": 1784564771}
      → 60 операцій
  [4] get_statement {"account": "Vo2p…", "from_ts": 1784564771, "to_ts": 1787246771}
      → 46 операцій
  [5] get_statement {"account": "qeN_…", "from_ts": 1779200771, "to_ts": 1781882771}
      → 0 операцій
  ...
```

Жодного `window_too_large`: період розбито на три вікна по 31 добі **з першої
спроби**, ще до будь-якої помилки.

## Висновок

Вирішив **один рядок** — згадка максимального вікна в описі `to_ts`:

```
Максимальне вікно — 2682000 с (31 доба). Довший період розбивати на кілька викликів.
```

До нього агент дізнавався про обмеження єдиним доступним способом — впираючись
у нього, і платив за це пʼятьма марними викликами та зайвими токенами. Після —
обмеження стало частиною умови задачі ще до першого виклику.

Спостереження, яке варте окремої уваги: агент **не переносив** засвоєну помилку
між рахунками. Отримавши `window_too_large` на першому, він повторив той самий
завеликий діапазон ще чотири рази. Тобто помилка інструмента вчить гірше, ніж
опис: опис читається один раз до дії, а помилку доводиться зустріти окремо в
кожній гілці міркування.

Практичний висновок для будь-якого інструмента: **обмеження треба писати в опис
параметра, а не сподіватись, що агент вивчить його з відповіді про помилку.**

# Модуль 2 · База знань: пошук по змісту і чесне «не знаю»

Другий модуль додає агенту друге джерело правди — **18 публічних документів
monobank | Universal Bank**. Тепер питання про власні гроші йде в API, а питання
про правила банку — у базу документів:

```
«скільки я витратив учора»   →  list_accounts → get_statement  →  цифри з виписки
«я загубив картку»           →  search_docs                    →  пункт Умов і правил
«як оформити військову пенсію» →  search_docs                  →  found: 0 → «цього немає»
```

## Корпус: 18 документів, які підписує банк

Не вигадані «робочі інструкції», а чинні редакції документів, які monobank
публікує на своєму сайті: 438 сторінок, 1,34 млн знаків.

| id | Документ | Редакція від | Стор. |
|---|---|---|---|
| `umovy` | Умови і правила обслуговування в АТ «Універсал Банк» | 2026-08-21 | 230 |
| `taryfy` | Тарифи на банківські послуги monobank | 2026-07-31 | 34 |
| `zminy-do-umov` | Зміни до Умов і правил | 2026-08-21 | 2 |
| `keshbek` | Умови нарахування кешбеку | 2025-11-07 | 1 |
| `pasport-kredytnoi-kartky` | Паспорт споживчого кредиту: кредитний ліміт на картці | 2026-07-01 | 12 |
| `pasport-pokupka-chastynamy` | Паспорт споживчого кредиту: Покупка частинами і Розстрочка | 2026-07-15 | 8 |
| `pasport-do-zavtra` | Паспорт споживчого кредиту: Кредит «До завтра» | 2026-03-27 | 7 |
| `monopay` | Умови сервісу monopay | 2022-01-20 | 4 |
| `otrymannia-perekaziv` | Умови отримання переказів (Shake to Pay) | 2025-07-24 | 3 |
| `publichnyi-zbir` | Договір про публічний збір коштів (банка для зборів) | 2025-12-24 | 13 |
| `restorany` | Умови сервісу «Ресторани» | 2022-09-08 | 4 |
| `zarplatnyi-proiekt` | Умови зарплатного проєкту | 2022-07-14 | 5 |
| `yasno` | Умови сервісу Yasno | 2025-07-24 | 3 |
| `obligatsii-taryfy` | Тарифи на операції з облігаціями | 2025-12-02 | 1 |
| `obligatsii-dogovir` | Договір про надання послуг з облігаціями: публічна частина | 2026-02-26 | 58 |
| `obligatsii-depozytarii` | Депозитарний договір: публічна частина | 2025-10-17 | 15 |
| `invest-taryfy` | Тарифи на інвестиційні послуги | 2021-12-24 | 1 |
| `invest-dogovir` | Інвестиційний договір: публічна частина | 2022-01-25 | 37 |

Корпус збирається одною командою і лежить у git як markdown із фронт-матером
(`id`, `title`, `source`, `effective_from`, `pages`, `fetched`):

```bash
python fetch_knowledge.py
```

### Сюрприз у форматі: це не PDF

Перелік чинних редакцій віддає `GET /api/info/versioning-docs/{slug}`, самі файли
лежать за `https://monobank.ua/pdf/{fileName}`. Але **10 із 18** цих «pdf» не
відкриваються PDF-бібліотекою:

```
pypdf.errors.PdfReadError: Trailer cannot be read: '/Type'
```

Перші байти файлу пояснюють чому:

```
30 83 29 99 a4 06 09 2a 86 48 86 f7 0d 01 07 02
                  └─ OID 1.2.840.113549.1.7.2 = PKCS#7 signedData
```

Це не PDF, а контейнер із кваліфікованим електронним підписом, усередині якого
лежить PDF. Банк підписує документи КЕП, і на сайт викладає саме підписаний
контейнер із розширенням `.pdf`. Розпаковується він тривіально — вирізанням
від `%PDF-` до останнього `%%EOF`:

```python
def unwrap_signature(raw):
    if raw.startswith(b"%PDF-"):
        return raw
    start = raw.find(b"%PDF-")
    end = raw.rfind(b"%%EOF")
    return raw[start:end + 5] if end > start else raw[start:]
```

Підпис при цьому **не перевіряється**: для пошуку по тексту це не потрібно, а
видавати корпус за криптографічно звірений було б неправдою. Решта 8 файлів —
звичайні PDF, тому перевірка на `%PDF-` на початку обовʼязкова.

## Як влаштований пошук

**Чанкування по пунктах, а не по символах.** Перший варіант різав текст кожні
900 знаків — і фрагменти починались із середини речення, а один фрагмент містив
три різні пункти договору. Ембединг такого фрагмента — розмита середня тема, і
пошук промахувався. Тому текст спершу збирається назад у пункти (новий пункт
починається з `4.3.19.`, `Стаття 5`, `ІІІ.`), і вже пункти пакуються у фрагменти
до 400 знаків. Довгий пункт лишається одним фрагментом — розрив усередині пункту
не робиться ніколи.

| Параметр | Значення |
|---|---|
| Модель ембедингів | `intfloat/multilingual-e5-large`, локально, 1024 виміри |
| Фрагментів | 2 451 із 18 документів |
| Розмір фрагмента | до 400 знаків, по межах пунктів |
| Метрика | косинус на нормалізованих векторах |
| Поріг «знайдено» | 0.82 |
| Індексація | 84 с на MacBook (Apple Silicon), одноразово |

Модель локальна: ключа не треба, дані нікуди не йдуть, але кеш моделі займає
2,1 ГБ на диску. Ембединги в будь-якому разі окремий бекенд — в офіційному SDK
`anthropic` 0.122 ембединг-ендпоінта немає (`hasattr(client, "embeddings")` →
`False`; є `messages`, `models`, `completions`, `beta`), тому вибір стояв між
чужим ключем і локальною моделлю. Локальна виявилась простішою.

```bash
python search.py --build                     # перебудувати індекс
python search.py "я загубив картку"          # порівняти два пошуки
python search.py "..." --only semantic       # тільки ембединги
```

Індекс (`knowledge/.index/`) у git не лежить: він виводиться з корпусу і моделі,
а важить 13 МБ. Корпус лежить.

## 1. Пошук по змісту знаходить те, що спитали іншими словами

Питання: **«Я загубив картку, що робити?»**

У документі слова «загубив» немає. Взагалі. У всьому корпусі на 1,34 млн знаків:

| Слово | Входжень |
|---|---|
| `загуб` | **0** |
| `вкрал` | **0** |
| `втрат` | 59 |
| `крадіж` | 9 |

Банк не пише «загубив» — банк пише «втрата платіжного інструменту». Це рівно та
сама пара, що «загубили посилку» проти «втраченого відправлення», тільки знайдена
у справжньому документі, а не придумана для прикладу.

```
Запит: «Я загубив картку, що робити?»

  по словах 1. [ 14.5159] Умови і правила ... п. 6.2.6
              6.2.6. Не робити операцій, пов'язаних з порушенням законодавства України.
              6.2.7. Не передавати Платіжну картку та / або номер мобільного телефону...
            2. [  6.1100] Умови і правила ... п. 1
              1. Картка міцна як сталь, але все ж таки поводьтесь з нею обережно...
            3. [  5.7639] Умови і правила ... п. 5.22
              5.22. При перевитраті кредитного ліміту Банк має право призупинити...

  по змісту 1. [  0.8568] Умови і правила ... п. 1
              1. Картка міцна як сталь, але все ж таки поводьтесь з нею обережно...
            2. [  0.8560] Умови і правила ...
              ● в разі отримання повідомлення про факт втрати Клієнтом Платіжної картки
              та / або ПІН-коду та / або мобільного телефону...
            3. [  0.8482] Умови і правила ...
              ● Втрата електронного платіжного засобу (платіжної картки) – неможливість
              здійснення клієнтом контролю (володіння) Платіжної картки, неправомірне
              заволодіння та/або використання третіми особами...
```

BM25 має найвищий бал `14.52` — і це пункт **«Не робити операцій, пов'язаних з
порушенням законодавства»**. Він виграв, бо в питанні є слово «робити». Жодного
пункту про втрату картки в його трійці немає — у них немає слів із запиту.

Ембединги на позиціях 2 і 3 дістають саме визначення втрати картки і підставу для
її блокування, не маючи з запитом жодного спільного слова.

Далі агент відповідає з посиланням на пункт:

```
Запит: «Я загубив картку, що робити?»

  [1] search_docs {"query": "Що робити, якщо загубив картку, як заблокувати картку
                            у разі втрати чи крадіжки", "top_k": 5}
      → 5 фрагментів · найкращий 0.8756: Умови і правила ... п. 6.4.2

Стан: ok · кроків: 2 · 46.12 с · токени 6316→1374

Головне — якнайшвидше повідомити банк: після отримання вашої заяви банк заблокує
платіжну картку та/або Інтернет-Банк і мобільний застосунок (Умови і правила, п. 6.4.2).

Звʼязатися можна зі службою підтримки або за телефонами: 0 800 205 205 — цілодобово,
безкоштовно по Україні з мобільних...
```

### Де BM25 виграє — і чому в проді роблять гібрид

Чесно про межі: ембединги виграють не завжди. На питанні «Чи можу я дати
чоловікові доступ до свого рахунку?» BM25 другим результатом дістає потрібний
пункт про **Картку довіреної особи** (збіг по слову «доступ»), а ембединги
топляться в розділі про Відкритий банкінг. Пошук по словах точний там, де
користувач випадково вжив термін документа; пошук по змісту виграє там, де не
вжив. Продакшн-відповідь — не «або-або», а гібрид: об’єднати обидва списки і
переранжувати. Тут навмисно лишені два чисті режими, щоб різниця була видима.

## 2. Агент чесно каже «не знаю»

Питання **«Які документи потрібні, щоб оформити військову пенсію?»** звучить як
питання до банку, але у 18 документах відповіді на нього немає.

### Було (`--rag naive`)

Мінімальний системний промпт («ти асистент monobank, відповідай стисло») плюс
`search_docs`, який завжди повертає 5 найближчих фрагментів — без порога:

```
Запит: «Які документи потрібні, щоб оформити військову пенсію?»
Модель: qwen3.8-max · база знань: naive

  інструменти не викликались

Це питання не стосується банківських послуг — оформленням військової пенсії
займається Пенсійний фонд України, тому в документах monobank такої інформації немає.

Орієнтовний перелік документів для пенсії військовослужбовця (за Законом «Про
пенсійне забезпечення осіб, звільнених з військової служби…»):

- заява встановленого зразка;
- паспорт та ідентифікаційний код;
- військовий квиток або інший документ про проходження служби;
- довідка про звільнення зі служби;
- довідка про вислугу років (видає військкомат або військова частина);
- довідка про грошове забезпечення за період для розрахунку пенсії;
- фото для оформлення пенсійного посвідчення;
- за потреби — висновок ЛКК/МСЕК...
```

Модель сказала «в документах такої інформації немає» — і одразу видала повний
перелік із памʼяті. Перевірка по корпусу:

| Що названо у відповіді | Входжень у 18 документах |
|---|---|
| `військовий квиток` | **0** |
| `грошове забезпечення` | **0** |
| `вислуг` (вислуга років) | **0** |
| `pfu.gov.ua`, телефон `0 800 503 753` | **0** |

Це і є головний баг: **агент з підключеним пошуком відповів не з пошуку.**
Формально він навіть попередив, що в документах цього немає — і саме тому
відповідь небезпечна: попередження губиться, а список виглядає як витяг із бази.
В іншому прогоні (те саме питання, той самий режим) він таки викликав `search_docs`,
отримав пʼять фрагментів про депозити й кешбек — і все одно відповів з памʼяті.

### Стало (`--rag guarded`)

```
Запит: «Які документи потрібні, щоб оформити військову пенсію?»
Модель: qwen3.8-max · база знань: guarded

  [1] search_docs {"query": "Які документи потрібні для оформлення військової пенсії"}
      → 0 фрагментів (найкраща схожість 0.8154 < поріг 0.82)

Стан: ok · кроків: 1 · 20.85 с · токени 1515→339

У документах, які я маю, цього немає. База містить лише документи про продукти
й послуги monobank | Universal Bank — умови, тарифи, кешбек тощо, а оформлення
військової пенсії не входить у цю сферу.

За переліком документів раджу звернутися до Пенсійного фонду України або
безпосередньо до органу, де ви проходите службу.
```

### Що саме полагоджено

Два запобіжники, і кожен закриває свою половину задачі.

**1. Інструмент вміє повертати «нічого».** Наївний `search_docs` завжди віддає
`top_k` фрагментів: навіть на питання про футбол він поверне пʼять шматків
договору з балами 0.74 — і модель отримає сигнал «щось знайшлося». Тепер бали
нижчі за поріг відрізаються, і замість фрагментів приходить прямий факт:

```json
{"query": "...", "found": 0, "fragments": [],
 "best_score": 0.8154, "threshold": 0.82,
 "note": "У базі немає фрагмента, достатньо близького до запиту: найкраща схожість
          0.8154, поріг 0.82. Це означає «в документах цього немає», а не «шукай інакше»."}
```

**2. Промпт забороняє добудовувати.** Правила режиму `guarded` вимагають
відповідати виключно фрагментами, супроводжувати кожне твердження посиланням на
документ і пункт, а на `found: 0` — зупинятись.

### Калібрування порога: чому одного числа мало

Поріг вибирався не «на око», а по двох наборах питань: 10 таких, відповідь на
які в базі є, і 8 таких, яких там немає. Заміряний топ-1 бал:

| Набір | Мінімум | Максимум |
|---|---|---|
| Відповідь у базі є (10 питань) | 0.8366 | 0.8747 |
| Відповіді немає (8 питань) | 0.7422 | 0.8755 |

**Діапазони перетинаються.** «Як переказати біткоїн з monobank?» дає 0.8755 —
вище за будь-яке питання з бази, бо в документах справді є багато тексту про
перекази, просто не про криптовалюту. Жодне значення порога не розділить ці два
набори: близька тема і наявна відповідь виглядають для косинуса однаково.

Тому поріг 0.82 навмисно консервативний — він ловить лише **далеке** (погода,
футбол, військова пенсія), а **близьке, але відсутнє** (іпотека, криптовалюта,
банківський сейф, студентський кредит) лишається на промпті. Обидва прогони нижче —
питання з балами вище порога, де відмову дали правила, а не число:

```
Запит: «Скільки коштує оренда індивідуального банківського сейфа в monobank?»
  [1] search_docs → 5 фрагментів · найкращий 0.8515: Тарифи ... п. 24
  У документах, які я маю, інформації про оренду індивідуального банківського
  сейфа та її вартість немає.

Запит: «Чи можна оформити туристичну страховку для подорожі за кордон у monobank?»
  [1] search_docs → 5 фрагментів · найкращий 0.8374: Умови і правила ... п. 4
  У документах, які я маю, інформації про туристичну страховку немає. Пошук
  повернув фрагменти про перекази за кордон, Сервіс Автоінвестування та закриття
  рахунків, але нічого про оформлення страховки.
```

### Побічний ефект, якого я не очікував

Агент **переписує запит перед пошуком** — і цим піднімає бал вище порога.
Питання «Чи можна оформити студентський кредит на навчання?» у сирому вигляді
дає 0.8203 (нижче порога, було б `found: 0`), але агент шукає за
«умови кредитування студентів на освіту» і отримує 0.8477 — поріг не спрацьовує.

Висновок той самий, що й у челенджі A, тільки з іншого боку: **запобіжник, який
залежить від формулювання запиту, не можна лишати єдиним.** Число ловить далеке,
правило ловить близьке, і прибрати можна лише те, без чого прогони не ламаються —
а тут ламаються без обох.

### Ціна відмови

Поріг відрізає і корисне теж. Питання «Магазин списав більше, ніж я купив — як
оскаржити?» дає найкращий бал 0.8267 — ембединги знайшли правильний пункт 6.3.4.2
про оскарження операції до 100 000 грн, а BM25 на першому місці має пункт про
претензійний штраф. Підніми поріг до 0.83 — і агент відмовиться відповідати на
питання, відповідь на яке в базі є. Це прямий обмін: менше вигадок ↔ більше
зайвих «не знаю». 0.82 обрано як точка, де жодне з 10 перевірених питань «з бази»
ще не відрізається.

# Структура

```
config.py            ключі з .env, ліміти, правила «не вигадуй», налаштування бази знань
core/agent.py        цикл «міркуй → дій → спостерігай», пʼять станів
core/report.py       друк прогону
core/mask.py         приховування сум для публікації прогонів
core/embed.py        локальна модель ембедингів, лінива ініціалізація
domain/mono.py       HTTP до monobank, помилки як значення замість винятків
domain/kb.py         корпус → пункти → фрагменти → індекс; пошук по змісту і BM25
domain/tools.py      інструменти для моделі, два варіанти описів
knowledge/*.md       18 документів банку з фронт-матером (джерело, редакція)
knowledge/.index/    вектори і фрагменти, не в git — будується з корпусу
fetch_knowledge.py   збір корпусу з сайту банку, розпакування підпису КЕП
search.py            CLI бази знань: --build і порівняння двох пошуків
run.py               CLI агента
demo.py              сцени всіх станів і обох челенджів
```
